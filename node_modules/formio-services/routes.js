'use strict';

const services = require('./services');
const config = require('./config');

module.exports = [
  {
    path: '/redis',
    service: services.redis,
    config: config.redis
  },
  {
    path: '/sheets',
    service: services.googlesheet,
    config: config.googlesheet
  }
];
