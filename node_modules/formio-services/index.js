'use strict';

require('dotenv').config();

const app = require('./server');

app.listen(process.env.PORT, () => console.log(`Form.io Services running on ${process.env.PORT}`)); // eslint-disable-line no-console
